const TIME_ARRAY = {
    HOUR : new Array(24 + 1).join('0').split(''),
    TWELVE_HOURS : new Array(12 + 1).join('0').split(''),
    MINUTES : new Array(60 + 1).join('0').split('')
}
export default TIME_ARRAY;